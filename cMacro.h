#ifndef CMACRO_INCLUDED
#define CMACRO_INCLUDED

#define INT(u) ((int*) (cPointer) u)
#define DOUBLE(u) ((double*) (cPointer) u)
#define FLOAT(u) ((float*) (cPointer) u)
#define CEDGE(u) ((cEdge*) (cPointer) u)
#define CVERT(u) ((cVert*) (cPointer) u)


#endif
